spring:
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
      data-locations: classpath:data.sql

  datasource:
    url: jdbc:postgresql://localhost:5436/aad_database
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
